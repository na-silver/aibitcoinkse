# 비트코인 매매 대시보드 - 필수 패키지
# GitHub 업로드용 버전 (트레이딩 의존성 제거)

# 웹 대시보드 프레임워크
streamlit>=1.28.0

# 데이터 분석 및 시각화
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0

# 비트코인 거래소 API (시장 데이터용)
pyupbit>=0.2.20

# 기술적 분석 (RSI, 이동평균 등)
ta>=0.10.2

# HTTP 요청 (API 호출용)
requests>=2.31.0

# 날짜/시간 처리
python-dateutil>=2.8.2

# 기타 유틸리티
typing-extensions>=4.5.0

# 주의: 다음 패키지들은 대시보드에서 제거됨
# - openai (AI 트레이딩 시스템에서만 사용)
# - google-search-results (뉴스 분석용, 대시보드에서 불필요)
# - youtube-transcript-api (선택사항, 대시보드에서 불필요)

# SQLite와 JSON은 Python 기본 라이브러리이므로 별도 설치 불필요 